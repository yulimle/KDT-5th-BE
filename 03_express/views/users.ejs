<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>
  <link rel="stylesheet" href="/css/style.css">
  <!--views 폴더안의 .. express가 허용하는 범위내에서만 가능하게 -->
  <script defer src="/js/main.js"></script>
</head>
<!-- USER_ARR, userCounts-->

<body>
  <h1>Hello, EJS World</h1>
  <h1>회원 목록</h1>
  <h2>총 회원 수 <%= userCounts %></h2>
  <ul>
    <% if(userCounts > 0) { %>
    <% for(let i =0; i < userCounts; i++) { %>
    <li>
      <p>ID: <%= USER_ARR[i].id%></p>
      <p>NAME: <%= USER_ARR[i].name%></p>
      <p>EMAIL: <%= USER_ARR[i].email%></p>
    </li>
    <% } %> <% } else { %>
    <li>회원 정보가 없습니다.</li>
    <% } %>
  </ul>
  <img src="/images/puppy.jpeg" alt="">
</body>

</html>